.env example for database settings
# Copy this to `.env` and fill values (do NOT commit secrets to git)
DB_DIALECT=postgresql
DB_DRIVER=psycopg2
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_DATABASE=your_database_name
